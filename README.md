# 선릿모닝 — 함께 쓰는 새벽

**포근나루(POGNARU STUDIO)의 새벽 글쓰기 클래스 모집 랜딩 페이지**

🌐 https://pognaru.github.io/sunlit-morning/

---

## 클래스 소개

새벽에 혼자 쓰기로 마음먹어도 대개 사흘이면 멈춥니다. 선릿모닝은 매일 아침 5시 50분, 줌에서 각자의 자리에 앉아 30분을 함께 쓰는 시간입니다. 혼자 쓰는 시간인데도 외롭지 않고, 그래서 이어집니다.

잘 쓰지 않아도 됩니다. 검열 없이 떠오르는 대로, 지우지 않고 멈추지 않고 씁니다. 그 안에서 **빛나는 돌멩이**를 건지는 것이 이 시간의 목적입니다.

| 구분 | 내용 |
|------|------|
| 기간 | 9월 1일 ~ 30일 (월~금) |
| 시간 | 새벽 5시 50분부터 30분간 |
| 방식 | 줌 (오디오 off, 화면 on) |
| 참가비 | 2만원 |
| 쉬는 날 | 토·일·법정공휴일 |

신청은 네이버 폼(`https://naver.me/FM9yEnM2`)으로 연결됩니다.

---

## 구조

```
sunlit-morning/
├─ index.html            # 모집 랜딩 — 처음 오시는 분께 (공개·검색 허용)
└─ continue/index.html   # 이어가기 안내 — 이미 함께 걷는 분께 (noindex)
```

두 장 모두 이미지·스크립트 없이 HTML 한 장으로 완결됩니다. 배경의 별과 구간을 잇는 파도는 전부 CSS·인라인 SVG입니다.

### 두 페이지의 역할이 다릅니다

| | 모집 랜딩 `/` | 이어가기 `/continue/` |
|---|---|---|
| 읽는 분 | 선릿모닝을 **처음 만나는** 분 | **이미 함께 걷고 있는** 분 |
| 하는 말 | 이런 시간입니다 (설명) | 다음 달도 함께 가시겠어요 (초대) |
| 검색 | 허용 | **`noindex`** — 링크로만 전합니다 |
| 폼 선택지 | 해당 월 | **'다음달 이어가기'** |

**이어가기 페이지에는 설명을 다시 넣지 않습니다.** 이미 아시는 분께 "선릿모닝은 이런 시간입니다"를 반복하면 안내가 아니라 홍보가 됩니다. 대신 지나온 시간을 짚고, 이어가는 법 세 단계와 다음 달 일정만 둡니다.

**쉬어가도 괜찮다는 자리를 반드시 남깁니다.** 재등록 페이지에서 가장 중요한 문단입니다. 이 한 구간이 없으면 페이지 전체가 재촉으로 읽힙니다 — 재촉은 선릿모닝의 결이 아닙니다.

## 배포

GitHub Pages — `main` 브랜치 루트(`/`)를 그대로 서빙합니다. 푸시하면 곧 반영됩니다. 워크플로·넷리파이 연동은 없습니다.

---

## 이 레포가 따로 있는 이유

이 페이지는 원래 디퍼 언브랜딩 과제로 만들어 [`deeper-haetsal`](https://github.com/POGNARU/deeper-haetsal)의 `assignment/sunlit-morning.html`에 있었습니다. 2026-07-17에 **독립 레포로 분리**했습니다.

과제 아카이브 안에 두면 주소가 `.../deeper-haetsal/assignment/sunlit-morning.html`이 되는데, 단축 주소로 공유해도 주소창에는 이 긴 주소가 남습니다. 뒷자리를 지우면 `/assignment/` 목록이 열려 **디퍼 과제 전부가 처음 온 손님에게 노출**됩니다. 모집 페이지가 개인 아카이브 안에 세 들어 사는 구조 자체의 문제라, 단축 주소로는 막을 수 없었습니다.

독립 레포로 옮기면 뒷자리를 지워도 `https://pognaru.github.io/` → 404. 나루의밤(`bamnaru`)이 이미 쓰던 방식 — **클래스는 자기 레포를 갖는다** — 과 같아졌습니다.

`deeper-haetsal` 쪽 사본은 과제 기록으로 그대로 남되 `noindex`가 걸렸고, 그 주소는 더 이상 공유하지 않습니다. **두 파일은 이제 각자 갑니다** — 모집 내용을 고칠 때는 이 레포만 고치면 됩니다.

## 운영 규칙

- **이어가기 페이지에 절대 넣지 않는 것** — 계좌번호(공개 URL입니다. 계좌는 문자로만 나갑니다) · 다이어리 《스무하루의 빛》(깜짝 선물이라 예고하지 않습니다) · "마감·서두르세요" · 기수 표현 · 받는 분의 이름이나 필명(주소 하나를 여러 분께 그대로 보냅니다).
- **주소를 고정해 매월 갱신합니다.** 기수마다 새 파일을 만들지 않습니다. 검색 신뢰가 쌓이고, 인스타 프로필 링크를 한 번만 걸면 되고, 이미 뿌린 링크가 늘 최신 달을 가리킵니다.
- 매달 바뀌는 것은 **모임 안내의 기간** 한 줄과, 폼을 새로 만들 경우 **신청 버튼 주소**입니다.
- 같은 문구가 인스타 카드 8번과 캡션(볼트 `클로드 에이전트/윤슬의 기록/260715 선릿모닝 인스타 카드뉴스.md`)에도 있으니 함께 맞춥니다.
- **검색을 허용합니다** (`noindex` 없음). 모집 페이지라 찾을 수 있어야 하고, 사적 정보가 없어 안전합니다. `og:url`은 이 레포 주소를 가리켜야 합니다 — 옛 주소가 남으면 공유 미리보기가 아카이브를 물고 갑니다.
- 푸터는 `POGNARU STUDIO`만 씁니다. 손님이 보는 자리라 디퍼를 병기하지 않습니다.
- **돌아가기 링크를 넣지 않습니다.** 공유용 단독 랜딩이라 링크를 받은 사람이 디퍼 아카이브로 타고 들어갈 자리를 만들지 않습니다. (`deeper-haetsal`에 남은 과제 기록 사본에는 `← 과제 목록`이 있습니다 — 그쪽은 아카이브 안에서만 도니까요.)

## 디자인 — '새벽' 팔레트

포근나루 크림 팔레트를 쓰지 않는 **예외 페이지**입니다. 선릿모닝은 '새벽'이 정체성이라, 페이지 자체가 새벽이 밝아오는 과정이 되도록 설계했습니다 (2026-07-16 햇살 요청 — "포근나루 컬러를 벗고 더 시각화된 페이지로").

| 구간 | 색 |
|---|---|
| 밤 | `#131F38` → `#22304F` |
| 여명 보라 | `#4E4066` |
| 노을 | `#8E5A62` → `#C97B5E` |
| 살구 | `#EBAE85` |
| 아침 | `#F7D98B` → `#FDF6E7` |

스크롤을 내릴수록 밤에서 아침으로 넘어가고, 구간 사이는 **파도 무늬 SVG**로 잇습니다.

글꼴 — 영문 타이틀: Playfair Display 이탤릭 · 큰 숫자 `5:50`: Cormorant Garamond(올드스타일) · 한글 헤딩: Gowun Batang · 본문: Nanum Myeongjo · 라벨: Gowun Dodum.

같은 시각 언어를 인스타 카드(볼트 `클로드 에이전트/나무의 기록/260715 선릿모닝 카드 디자인/v2/`)와 공유해 페이지와 카드가 한 세트로 읽힙니다.

---

*고요히, 마음에 머무는 글을 전하는 **포근나루***

## 파비콘

브라우저 탭 아이콘으로 포근나루 심볼(나룻배)을 쓴다. 2026-07-22 적용, 같은 날 심볼을 새 시안으로 교체하며 다시 뽑았다. 2026-07-23에 노 모양을 다듬어(자루 연장·이음매 정리 → 노 전체를 한 축으로) 최종본으로 다시 교체했다.

```
favicon-32.png    탭 아이콘
favicon-180.png   애플 터치 아이콘
```

레포 루트에 두고, 모든 HTML의 `<head>`에 상대경로로 링크한다. 하위 폴더 문서는 `../favicon-32.png`처럼 `../`를 붙인다 — GitHub Pages 프로젝트 사이트는 `/레포명/` 아래로 서빙되므로 루트 절대경로를 쓰면 안 된다.

원본은 볼트 `클로드 에이전트/브랜드/로고/`에 있다.
